name: OKE Multi-Region Cluster Deployment

on:
  pull_request:
    branches:
      - main
    paths:
      - 'environments/**'
      - 'modules/**'
      - '.github/scripts/**'
      - '.github/argocd/**'
      - '.github/config.sh'
      - '.github/workflows/oke-deploy.yaml'
  push:
    branches:
      - main
    paths:
      - 'environments/**'
      - 'modules/**'
      - '.github/scripts/**'
      - '.github/argocd/**'
      - '.github/config.sh'
      - '.github/workflows/oke-deploy.yaml'

  workflow_dispatch:
    inputs:
      action:
        description: 'Choose action to run'
        required: true
        default: 'plan'
        type: choice
        options:
          - plan
          - apply
          - plan_and_apply

permissions:
  contents: read
  pull-requests: write

concurrency:
  group: oke-deploy-${{ github.ref }}
  cancel-in-progress: false

jobs:
  # ============================================================
  # Job 1: OCI Infrastructure (VCN, OKE, etc.)
  # ============================================================
  oke-deploy:
    name: "OKE Provisioning - ${{ matrix.region }}"
    runs-on: ubuntu-latest
    strategy:
      fail-fast: false  
      matrix:
        include:
          - region: us-ashburn-1
            environment: production-us-ashburn
            tf_workspace: oke-us-ashburn
            working_dir: environments/us-ashburn

    environment: ${{ matrix.environment }}
    outputs:
      cluster_id: ${{ steps.get_cluster_id.outputs.cluster_id }}
      nlb_public_ip: ${{ steps.get_cluster_id.outputs.nlb_public_ip }}

    steps:
      - name: Checkout
        uses: actions/checkout@v4
        with:
          fetch-depth: 0

      - name: Check for changes in oke
        id: filter
        env:
          GITHUB_EVENT_NAME: ${{ github.event_name }}
          WORKFLOW_INPUT_ACTION: ${{ github.event.inputs.action }}
        run: |
          chmod +x ./.github/scripts/check_changes.sh
          ./.github/scripts/check_changes.sh ${{ matrix.working_dir }}

      - name: Setup Terraform
        if: |
          steps.filter.outputs.run_plan == 'true' || 
          steps.filter.outputs.run_apply == 'true'
        uses: hashicorp/setup-terraform@v3

      - name: Debug - Verify Module Path
        if: |
          steps.filter.outputs.run_plan == 'true' || 
          steps.filter.outputs.run_apply == 'true'
        run: |
          echo "Current directory: $(pwd)"
          echo "Working directory: ${{ matrix.working_dir }}"
          echo "Module path check from ${{ matrix.working_dir }}:"
          cd ${{ matrix.working_dir }}
          echo "  pwd: $(pwd)"
          echo "  ../../modules exists: $(test -d ../../modules && echo 'YES' || echo 'NO')"
          echo "  ../../modules/oke exists: $(test -d ../../modules/oke && echo 'YES' || echo 'NO')"
          ls -la ../../modules/oke/

      - name: Terraform Init
        if: |
          steps.filter.outputs.run_plan == 'true' || 
          steps.filter.outputs.run_apply == 'true'
        run: terraform init
        working-directory: ${{ matrix.working_dir }}
        env:
          TF_TOKEN_app_terraform_io: ${{ secrets.TF_CLOUD_TOKEN }}

      - name: Generate CI Terraform Variables
        if: |
          steps.filter.outputs.run_plan == 'true' || 
          steps.filter.outputs.run_apply == 'true'
        working-directory: ${{ matrix.working_dir }}
        run: |
          cat > ci.auto.tfvars <<EOF
          compartment_id = "${{ secrets.OCI_COMPARTMENT_OCID }}"
          tenancy_ocid = "${{ secrets.OCI_TENANCY_OCID }}"
          user_ocid = "${{ secrets.OCI_USER_OCID }}"
          fingerprint = "${{ secrets.OCI_FINGERPRINT }}"
          private_key = <<EOT
          ${{ secrets.OCI_PRIVATE_KEY }}
          EOT
          region = "${{ vars.OCI_REGION }}"
          environment = "${{ matrix.environment }}"
          EOF
          
      - name: Terraform Plan
        if: |
          steps.filter.outputs.run_plan == 'true' || 
          steps.filter.outputs.run_apply == 'true'
        run: terraform plan -no-color -input=false
        working-directory: ${{ matrix.working_dir }}
        env:
          TF_TOKEN_app_terraform_io: ${{ secrets.TF_CLOUD_TOKEN }}

      - name: Terraform Apply
        if: steps.filter.outputs.run_apply == 'true'
        run: terraform apply -auto-approve -input=false
        working-directory: ${{ matrix.working_dir }}
        env:
          TF_TOKEN_app_terraform_io: ${{ secrets.TF_CLOUD_TOKEN }}

      - name: Export Cluster ID
        id: get_cluster_id
        if: steps.filter.outputs.run_apply == 'true'
        run: |
          CLUSTER_ID=$(terraform output -raw cluster_id)
          NLB_PUBLIC_IP=$(terraform output -raw nlb_public_ip)
          echo "cluster_id=$CLUSTER_ID" >> $GITHUB_OUTPUT
          echo "nlb_public_ip=$NLB_PUBLIC_IP" >> $GITHUB_OUTPUT
        working-directory: ${{ matrix.working_dir }}
        env:
          TF_TOKEN_app_terraform_io: ${{ secrets.TF_CLOUD_TOKEN }}

      - name: Save Terraform Plan (oke)
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: oci-terraform-plan-${{ github.run_number }}
          path: oci/terraform.tfplan
          retention-days: 7
          if-no-files-found: ignore

      - name: Job Summary
        if: always()
        run: |
          echo "## OCI Provisioning Summary" >> $GITHUB_STEP_SUMMARY
          echo "" >> $GITHUB_STEP_SUMMARY
          echo "- **Cluster ID**: ${{ steps.get_cluster_id.outputs.cluster_id }}" >> $GITHUB_STEP_SUMMARY
          echo "- **NLB IP**: ${{ steps.get_cluster_id.outputs.nlb_public_ip }}" >> $GITHUB_STEP_SUMMARY

  # ============================================================
  # Job 2: OCI and Kubernetes Configuration
  # ============================================================
  argocd:
    name: "Deploy ArgoCD & Bootstrap project"
    needs: oke-deploy
    runs-on: ubuntu-latest
    if: | 
      always() && 
      needs.oke-deploy.result == 'success' &&
      needs.oke-deploy.outputs.cluster_id != '' &&
      needs.oke-deploy.outputs.nlb_public_ip != ''
    steps:
      - name: Checkout
        uses: actions/checkout@v4
        with:
          fetch-depth: 0

      - name: Check for changes in .github
        id: filter
        env:
          GITHUB_EVENT_NAME: ${{ github.event_name }}
          WORKFLOW_INPUT_ACTION: ${{ github.event.inputs.action }}
        run: |
          chmod +x ./.github/scripts/check_changes.sh
          ./.github/scripts/check_changes.sh ".github"

      - name: Install OCI CLI & kubectl
        if: |
          steps.filter.outputs.run_plan == 'true' ||
          steps.filter.outputs.run_apply == 'true'
        run: |
          sudo apt-get update -y
          sudo apt-get install -y python3-pip
          pip3 install --upgrade oci-cli

      - name: Setup OCI credentials
        if: |
          steps.filter.outputs.run_plan == 'true' ||
          steps.filter.outputs.run_apply == 'true'
        env:
          OCI_TENANCY: ${{ secrets.OCI_TENANCY_OCID }}
          OCI_USER: ${{ secrets.OCI_USER_OCID }}
          OCI_FINGERPRINT: ${{ secrets.OCI_FINGERPRINT }}
          OCI_PRIVATE_KEY: ${{ secrets.OCI_PRIVATE_KEY }}
          OCI_REGION: ${{ vars.OCI_REGION }}
          DEFAULT_REGION: ${{ vars.OCI_DEFAULT_REGION }}
        run: |
          chmod +x ./.github/scripts/setup_oci_credentials.sh
          ./.github/scripts/setup_oci_credentials.sh

      - name: Generate Kubeconfig
        if: |
          steps.filter.outputs.run_plan == 'true' ||
          steps.filter.outputs.run_apply == 'true'
        env:
          CLUSTER_ID: ${{ needs.oke-deploy.outputs.cluster_id }}
          REGION: ${{ vars.OCI_REGION }}
        run: |
          chmod +x ./.github/scripts/kubeconfig.sh
          ./.github/scripts/kubeconfig.sh

      - name: Setup TLS Secret
        if: |
          steps.filter.outputs.run_plan == 'true' ||
          steps.filter.outputs.run_apply == 'true'
        env:
          SECRETS_PRIVATE_KEY: ${{ secrets.TLS_KEY_PEM }}
          SECRETS_PUBLIC_KEY: ${{ secrets.TLS_CERT_PEM }}
        run: |
          chmod +x ./.github/scripts/configure_tls_secret.sh
          ./.github/scripts/configure_tls_secret.sh

      - name : Install ArgoCD and Kubeseal Sealed Secrets
        if: |
          steps.filter.outputs.run_plan == 'true' ||
          steps.filter.outputs.run_apply == 'true'
        run: |
          chmod +x ./.github/scripts/install.sh
          ./.github/scripts/install.sh

      - name: Start Argocd
        if: |
          steps.filter.outputs.run_plan == 'true' ||
          steps.filter.outputs.run_apply == 'true'
        run: |
          chmod +x ./.github/scripts/argocd_start.sh
          ./.github/scripts/argocd_start.sh

      - name: Update ArgoCD Initial Admin Password
        if: |
          steps.filter.outputs.run_plan == 'true' ||
          steps.filter.outputs.run_apply == 'true'
        env:
          NLB_PUBLIC_IP: ${{ needs.oke-deploy.outputs.nlb_public_ip }}
          ARGOCD_ADMIN_PASSWORD: ${{ secrets.ARGOCD_ADMIN_PASSWORD }}
        id: update_argocd_password
        run: |
          chmod +x ./.github/scripts/argocd_updatepwd.sh
          ./.github/scripts/argocd_updatepwd.sh

      - name : Argocd Configuration
        if: |
          steps.filter.outputs.run_plan == 'true' ||
          steps.filter.outputs.run_apply == 'true'
        run: |
          chmod +x ./.github/scripts/argocd_sa_config.sh
          ./.github/scripts/argocd_sa_config.sh

      - name: K8s Setup Summary
        if: |
          success() &&
          (steps.filter.outputs.run_plan == 'true' ||
          steps.filter.outputs.run_apply == 'true')
        run: |
          echo "## Kubernetes Setup Complete" >> $GITHUB_STEP_SUMMARY
          echo "" >> $GITHUB_STEP_SUMMARY
          echo "✅ ArgoCD deployed successfully" >> $GITHUB_STEP_SUMMARY
          echo "✅ K8s RBAC configured" >> $GITHUB_STEP_SUMMARY
          echo "" >> $GITHUB_STEP_SUMMARY
          echo "Access ArgoCD at: \`https://${{ needs.oke-deploy.outputs.nlb_public_ip }}:30443\`" >> $GITHUB_STEP_SUMMARY
      
      - name: Checkout cluster-config repo
        uses: actions/checkout@v4
        with:
          repository: schmhj/cluster-config
          ref: main
          token: ${{ secrets.GH_PAT_SECRET_ADMIN }}
          
      - name: Apply AppProjects App
        run: kubectl apply -f bootstrap/prod/appprojects-app.yaml

      - name: Apply Root App
        run: kubectl apply -f bootstrap/prod/root-app.yaml

      - name: Bootstrap Summary
        if: success()
        run: |
          echo "## ArgoCD Bootstrap Complete" >> $GITHUB_STEP_SUMMARY
          echo "" >> $GITHUB_STEP_SUMMARY
          echo "✅ AppProjects app deployed" >> $GITHUB_STEP_SUMMARY
          echo "✅ Root app deployed" >> $GITHUB_STEP_SUMMARY  
      
  